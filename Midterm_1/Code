; Variable values: result = (var1 + 2) / (var3 - var2)
; result = (10 + 2) / (20 - 4) = 12 / 16 = 0 

section .data
    var1 dd 10
    var2 dd 4  
    var3 dd 20
    
    msg db 'Result: ', 0
    newline db 10, 0

section .bss
    result resd 1

section .text
    global _start
